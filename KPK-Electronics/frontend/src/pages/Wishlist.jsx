import React from 'react'; import {Link} from 'react-router-dom'; import ProductCard from '../components/ProductCard'; import {useApp} from '../context/AppContext'; export default function Wishlist(){const {wishlist}=useApp();return <main className="section wishlist-page"><p className="eyebrow">SAVED FOR LATER</p><h1>Your wishlist.</h1>{wishlist.length?<div className="product-grid">{wishlist.map((p,i)=><ProductCard p={p} index={i} key={p._id}/>)}</div>:<div className="empty-state"><h2>No saved pieces yet.</h2><p>Tap the heart on any appliance you want to keep close.</p><Link className="btn dark" to="/shop">Explore the collection</Link></div>}</main>}
